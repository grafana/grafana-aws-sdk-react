pre-commit:
  parallel: true
  commands:
    frontend-lint:
      glob: '*.{js,ts,tsx}'
      run: |
        npm run lint -- {staged_files}
        npx prettier --write {staged_files}
      stage_fixed: true
    spellcheck:
      run: npm run spellcheck -- {staged_files}
